# Custom Pack Images

Place card images here and reference them from `cards.csv` with paths relative
to this directory.

Examples:

```text
BT-CUSTOM-001.png
starter/BT-CUSTOM-002.webp
```

For transport, zip the whole `custom_pack/` directory so `pack.json`,
`cards.csv`, and `images/` stay at the root of the archive.
